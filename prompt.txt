Background:
In this workspace we have multiple Terraform files inside unity_catalog directory
which are used to create aws and databricks related resources. 
Now, the issue is that these terraform scripts are not properly segregated based on environments,
they are used to create resources for qa, dev, and prod. We have to identify all such resources which belong 
to dev and then safely move them to a different folder. 

Excpecation:
- Every databricks related resource will move to infrastructure/terraform/aws/environments/databrocks
- And, every aws related resource (non-databricks) will move to nfrastructure/terraform/aws/environments/data-databricks-dev
- Before moving, we have to safely identify the right place where this can be moved without affecting
   the existing functonality of source and destination terraform scripts.


Objective:
You need to identify all such resoueces in unity_catalog that belongs to dev environment and needs to be moved.
Once you have listed out, please create a md file with detailed information of resources that needs to be moved.
I will read and review this document first.

Implemetation Strategy:
This looks like a very big task and implmenting it in a single go will not be a good idea since a lot of 
things might break after migration, making it difficult for me to isolate the root cause of failure.
Hence, I want you to group such resources and come up with a 3 stage plan to implement the migration.
such that, we can execute stage 1, then i can test and if everything is ok then we move to next stage and so on.

