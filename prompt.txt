I want to integrate an AWS Route 53 HTTPS health check for Databricks into my existing Terraform module. 

Please modify my existing files to include this infrastructure:

1. Existing [main.tf / resources.tf]: Append an `aws_route53_health_check` pointing to a Databricks workspace URL, an `aws_cloudwatch_metric_alarm` tracking `HealthCheckStatus`, and an `aws_sns_topic` with an `aws_sns_topic_subscription` for email alerts.
2. Existing [variables.tf]: Add variables for the Databricks endpoint, the health check path (defaulting to "/login.html"), and the alert email.
3. Existing [outputs.tf]: Expose the health check ID and SNS topic ARN.

Requirements:
- Make sure to use `for_each` or dynamic blocks if this needs to handle multiple workspaces (Optional: Specify if you want this).
- Ensure the Route 53 resources and alarms are explicitly tied to the `us-east-1` provider if the rest of the module runs in a different region.
- Match the naming conventions, linting, and formatting of my existing code.

Here is my current module code:
[Paste your existing main.tf, variables.tf, and outputs.tf here]
