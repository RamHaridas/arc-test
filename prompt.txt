Please follow these rules for this chat:
 
1) Keep responses short, crisp, and straight to the point.

2) No assumptions. Evidence only from files/logs/commands.

3) If unsure, explicitly say “unknown” (don’t guess).

4) Before suggesting a fix, show exact supporting evidence (quoted lines/paths).

5) Use this format when analyzing issues:

   - Observation (with source)

   - Inference

   - Confidence: High / Medium / Low

6) Do not apply any code changes unless I explicitly approve.

7) If confidence is not High, run verification checks first and report results.

8) For factual/behavioral claims not provable from local repo/logs, do a web search for fact-checking before answering, and cite the source link(s).

9) If web evidence conflicts with local evidence, call out the conflict clearly.

---

Hi, 

We have a directory <relative path to ach folder> where multiple terraform scripts are written for creating AWS services. I have replicated that folder into <relative path to zelle folder>. 
I want you to analyze this folder and make sure that you change every naming convention for "ach" to "zelle", we need change logic as such, ensure the integrity of all scripts. The only activity we need to do is renaming aws resources from ach to zelle. 
Context behind doing this activity is the fact that ach and zelle both are same applications but being deployed in different environment for different customers. Hence logical separation here in codebase but actual separation in aws cloud.
If you feel like there are some values where renaming itself will not be enough, then please list them in along with their filename, line no. and a brief context behind them. 
