Role & Goal:
You are an expert AWS Terraform and Python developer. I have an existing Terraform module for AWS ARC (Application Recovery Controller) that currently acts as a simple alerting mechanism. I need you to upgrade this module into a flexible "failover orchestration framework."

Current State of the Terraform Module:
1. Reads an ARC cluster ARN as a variable.
2. Creates a CloudWatch alarm to listen for an ARC routing control failover event.
3. On alarm, it sends an alert to an SNS topic. 
4. Scan the workspace to understand more, above points may or may not be accurate.
*Note: There is currently NO Lambda function present in the module.*

Required Updates:
Please modify the Terraform module to implement the following orchestration architecture:

1. Add Optional Configuration Variables:
   - Create a boolean variable `enable_failover_scripts` (default: false).
   - Create a list of strings variable `failover_scripts` (default: []) where users can define specific recovery scripts they need to run.

2. Add an Orchestrator AWS Lambda Function:
   - Provision a new AWS Lambda function that will act as the "brain" of the failover orchestration.
   - Configure the existing CloudWatch event/alarm (or the existing SNS topic) to trigger this Lambda function.
   - Pass the new variables (`enable_failover_scripts` and `failover_scripts`) into the Lambda function as environment variables.

3. Configure IAM Permissions:
   - Create an IAM Role and Policy for the Lambda function.
   - The policy must grant the Lambda basic execution rights, permission to publish notifications, and generic permissions to update infrastructure via API calls (you can use placeholder permissions for the infrastructure updates, adding comments on where I should restrict them).

4. Provide the Lambda Source Code Scaffolding (Python):
   - Write the handler code that:
     a) Reads the environment variables to check if `enable_failover_scripts` is set to true.
     b) If true, parses the `failover_scripts` list and executes them in sequential order (simulate this logic if actual scripts are external).
     c) Sends out a notification (e.g., to Datadog or the existing SNS topic) once the recovery orchestration completes successfully or if it fails.

Output Requirements:
- Provide the updated Terraform configuration files.
- Provide the source code for the Lambda function (lambda_function.py).
- Ensure the code is production-ready, well-commented, and keeps the automation strictly optional so teams who only want monitoring alerts aren't forced into running scripts.
